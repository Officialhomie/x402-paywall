/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable experimental features if needed for middleware
  experimental: {
    // Add any experimental features here if required by x402-express
  },
}

module.exports = nextConfig

